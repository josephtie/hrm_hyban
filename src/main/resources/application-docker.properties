# Configuration pour l'environnement Docker
spring.datasource.url=jdbc:postgresql://postgres:5432/hyban_db_v2
spring.datasource.username=directus
spring.datasource.password=directus
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Configuration Keycloak pour Docker
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://192.168.1.2:8080/realms/hyban
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

jwt.auth.converter.resource-id=hrm_frontend
jwt.auth.converter.principal-attribute=preferred_username

# Keycloak admin settings
keycloak.admin.host=http://192.168.1.2:8080
keycloak.admin.realm=hyban
keycloak.admin.clientId=keycloak-admin-client
keycloak.admin.clientSecret=b6cFLwyL2MakdzHxomjsamxesop9IbIE

# Configuration serveur
server.port=7200

# Upload directory
app.upload-dir=/app/uploads

# Configuration CORS pour Docker
spring.web.cors.allowed-origins=http://localhost:7153,http://192.168.1.2:7153,http://frontend:80,http://nginx:80
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# Configuration mail (peut être remplacée par des variables d'environnement)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:ybijoseph@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Jq,h6!esFyXEmv_}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Swagger/OpenAPI
springdoc.show-actuator=true
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Logging
logging.level.root=INFO
logging.level.com.nectux.mizan=DEBUG

# Carbone service
carbone.url=http://localhost:4000/render
